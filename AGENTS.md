# 공통 작업 지시

- 사용자 승인 없이 repo 밖 절대 경로에 파일을 만들거나 쓰지 않는다.
- 테스트 산출물은 가능한 repo 내부의 명시된 경로에만 만들고, 임시 산출물을 만들면 사용자에게 알린다.
- C 드라이브, 사용자 홈, AppData 같은 위치를 도구나 라이브러리가 자동으로 쓰려 할 때는 repo 내부 경로로 우회하거나 먼저 알린다.
- 사용자가 "모든 오류를 해결해서 쓰고 싶다"고 말하면 단순 skip이 아니라 원인별 복구 방법을 우선 찾는다.
- 모델/가중치 파일은 자동 다운로드보다 명시적 로컬 경로를 선호하고, 필요한 파일명과 위치를 분명히 안내한다.
- `sys.exit()` 또는 `raise SystemExit`로 IDE 실행을 끊지 말고, 가능하면 `main() -> int` 반환 방식으로 종료 상태를 표현한다.
- CSV 등 실험 결과 파일은 기존 값 규칙을 보존하고, 콘솔 요약/성공률 같은 부가 정보는 출력 전용으로 둔다.
- Windows + Anaconda 환경에서는 패키지 버전, 모델 파일 존재 여부, API 변경 여부를 먼저 확인한다.
- 사용자 터미널 화면을 직접 볼 수 있다고 가정하지 말고, 직접 실행한 명령 출력만 근거로 말한다.
- 학습용으로 유지보수하기 쉽게, 프로젝트 산출물에 직접 영향을 주는 실행 코드는 큰 단일 파일로 만들지 않는다.
- 가능한 한 실행 코드 파일은 주석, 문서 문자열, 빈 줄을 제외한 실제 동작 코드 기준으로 파일당 100줄 이하로 나누고, 모듈 간 연결은 `import`로 명시한다.
- 각 모듈에는 학습자가 흐름을 따라갈 수 있도록 핵심 의도와 입출력을 설명하는 주석을 충분히 남긴다.
- 모듈이 여러 개로 나뉘면 repo 최상위 `ABSORB.md`에 구조 다이어그램과 파일 간 연결 관계를 기록한다.
- 가능하면 lint 실행 시 불필요한 경고가 나오지 않도록 깔끔한 코드 스타일을 유지한다.
